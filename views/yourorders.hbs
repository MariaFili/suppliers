<h1>{{this.name}}</h1>

  <h2>Мои заказы</h2>
<div> {{#each addsinfo}}


  <table class="table table-striped table-bordered" style="width:100%">
    <thead class="thead-dark">
    <tr>
      <th><a href="/ad/{{this.id}}">
        {{this.title}}
      </a></th>
      <th>{{this.district}}</th>
      <th>{{this.city}}</th>
      {{!-- <th><button>Посмотреть отклики</button></th> --}}
    </tr>
</thead>
{{#each this.responses}}

    <tr>
      <td>{{this.authorName}}</td >
      <td>{{this.price}}</td>
      <td>
        
<form method="post" action="/ad/{{this.id}}"> <input type="submit" value="Выбрать поставщика" tabindex="3"
        class={{this.id}}>
</form>

      </td>
    </tr>
{{/each}}
  </table>
  {{/each}}
</div>